---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: xaccesspolicies.agentic.networking.x-k8s.io
spec:
  group: agentic.networking.x-k8s.io
  names:
    kind: XAccessPolicy
    listKind: XAccessPolicyList
    plural: xaccesspolicies
    singular: xaccesspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Accepted')].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=='ResolvedRefs')].status
      name: ResolvedRefs
      type: string
    - jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: XAccessPolicy is the Schema for the xaccesspolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of XAccessPolicy
            properties:
              action:
                type: string
              externalAuth:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              rules:
                items:
                  properties:
                    authorization:
                      properties:
                        cel:
                          properties:
                            expression:
                              type: string
                          required:
                          - expression
                          type: object
                        mcp:
                          properties:
                            mcpBaseProtocolMethodsOption:
                              type: string
                            methods:
                              items:
                                properties:
                                  name:
                                    type: string
                                  params:
                                    items:
                                      type: string
                                    type: array
                                required:
                                - name
                                type: object
                              type: array
                          type: object
                        type:
                          type: string
                      required:
                      - type
                      type: object
                    name:
                      type: string
                    source:
                      properties:
                        serviceAccount:
                          properties:
                            name:
                              type: string
                            namespace:
                              type: string
                          required:
                          - name
                          type: object
                        spiffe:
                          type: string
                        type:
                          type: string
                      required:
                      - type
                      type: object
                  required:
                  - name
                  - source
                  type: object
                type: array
              targetRefs:
                items:
                  description: LocalPolicyTargetReferenceWithSectionName identifies
                    a target resource and optionally a specific section.
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    sectionName:
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  type: object
                type: array
            required:
            - action
            - targetRefs
            type: object
          status:
            description: status defines the observed state of XAccessPolicy
            properties:
              conditions:
                description: conditions represent the current state of the XAccessPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
